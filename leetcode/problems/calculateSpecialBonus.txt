# https://leetcode.com/problems/calculate-special-bonus/description/
# My solution

SELECT 
    employee_id,
    IF(
        MOD(employee_id, 2) = 1 AND name NOT LIKE 'M%', salary, 0
    ) as bonus
FROM Employees
ORDER BY employee_id;