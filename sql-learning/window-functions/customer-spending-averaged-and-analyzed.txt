# https://www.practicewindowfunctions.com/problem/5/customer-spending-averaged-and-analyzed

SELECT
	customerid,
	orderid,
	totalamount,
	AVG(totalamount) OVER(	
		PARTITION BY customerid
	) as averageordervalue
FROM orders;